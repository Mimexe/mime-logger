# Examples

This folder contains example code for using Mime Logger. You need to build the project first (`pnpm run build`) to run these examples, as they import the built ESM/CJS files from the `dist` folder.
